password = int(input("Enter the password"))
if len(password) < 8:
    print("The password is too short")
elif password.isalpha() or password.isdigit():
     print("the password should contain mixture of letters and num")
else:
     print("strong password")
     